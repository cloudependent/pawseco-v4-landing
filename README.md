# PawsEco — Landing Page (V4)
This repo hosts the public marketing one-pager for **PawsEco** on **https://pawsV4.pawseco.com.au** via GitHub Pages.

**Note**: The existing website remains on https://pawseco.com.au - this new landing page is on a subdomain.

## Quick edit
- Edit `index.html`. Commit to `main`. Pages auto-deploys.

## SEO assets
- `robots.txt` — allows indexing.
- `sitemap.xml` — lists https://pawsV4.pawseco.com.au/
- `site.webmanifest` — PWA metadata (icons can be swapped later).
- `favicon.ico` — placeholder.

## DNS
See `deploy-log.txt` for A/AAAA and CNAME records to set on GoDaddy.

